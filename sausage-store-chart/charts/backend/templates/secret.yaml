apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-secret
type: Opaque
stringData:
  mongo_uri: {{ .Values.env.mongoUri }}
  postgres_password: {{ .Values.env.postgresPassword }}
